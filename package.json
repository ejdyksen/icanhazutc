{
  "name": "icanhazutc",
  "version": "1.0.0",
  "description": "A dead simple Cloudflare Worker that displays the current UTC time",
  "main": "src/worker.js",
  "scripts": {
    "build": "mkdir -p dist && html-minifier-terser --remove-comments --output dist/index.html src/index.html && cp src/worker.js dist/",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --cwd dist",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "utc",
    "time"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "wrangler": "^4.26.1"
  }
}
