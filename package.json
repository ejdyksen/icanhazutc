{
  "name": "icanhazutc",
  "version": "1.0.0",
  "description": "Display the current UTC time in plain text",
  "main": "src/worker.js",
  "scripts": {
    "build": "mkdir -p dist && html-minifier-terser --remove-comments --minify-js --minify-css --output dist/index.html src/index.html && cp src/worker.js dist/",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --cwd dist",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "utc",
    "time"
  ],
  "author": "E.J. Dyksen",
  "license": "MIT",
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "wrangler": "^4.30.0"
  }
}
